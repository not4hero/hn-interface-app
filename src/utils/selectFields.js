export const selectFields = ({id, by, url, time, title, score, text, kids, descendants} = {}) => ({
    id, 
    by, 
    url, 
    time, 
    title,
    score,
    text,
    kids,
    descendants
});